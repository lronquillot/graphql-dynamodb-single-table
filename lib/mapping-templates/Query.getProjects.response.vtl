#if($ctx.results.items.size() == 0)
    #return
#end

#set($projects = [])
#set($activities = [])

#foreach($item in $ctx.result.items)
    #if($item["_TYPE"] == "PROJECT")
        $util.qr($projects.add($item))
    #elseif($item["_TYPE"] == "ACTIVITY")
        $util.qr($activities.add($item))
    #end
#end

$util.qr(
    $projects.put("activities", $activities)
)

$util.toJson($projects)