#if($ctx.results.items.size() == 0)
    #return
#end

#set($conversation = {})

#foreach($item in $ctx.result.items)
    #if($item["_TYPE"] == "CONVERSATION")
        #set ($conversation = $item)
    #end
#end


$util.toJson($conversation)